p#notice = notice

p
  strong Name:
  = @channel.name

= link_to_next_page @messages, "Next"

table.table
  thead
    tr
      th At
      th Name
      th Text

  tbody
    - @messages.reverse_each do |message|
      tr
        td = display_time(message.published_at)
        td = message.user.name
        td = message.text

= link_to_previous_page @messages, "Prev"

= link_to 'Back', channels_path
